<section class="half">

<h1>Editing cohort</h1>

<%= form_for @cohort, url: admin_cohort_path(@cohort) do |f| %>
<%= render partial: 'form', locals: {f: f} %>
<% end %>

<%= link_to 'Show', @cohort %> |
<%= link_to 'Back', admin_cohorts_path %>

</section>