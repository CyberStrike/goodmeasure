<aside>
<section id="back"> <%= link_to 'Back', cohort_path(params[:cohort_id]) %> </section>
	<h1></h1>
</aside>

<section id="main">

<p class="small-header"> New Unit</p>
 <section class="module">

<%= render 'form_new' %>
</section>
</section>