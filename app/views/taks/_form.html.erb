<%= form_for(@tak) do |f| %>
  <% if @tak.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tak.errors.count, "error") %> prohibited this tak from being saved:</h2>

      <ul>
      <% @tak.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :visibility %><br>
    <%= f.text_field :visibility %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
