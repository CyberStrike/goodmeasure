<%= form_for [@cohort, @blocker] do |f| %>
  <% if @blocker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blocker.errors.count, "error") %> prohibited this blocker from being saved:</h2>

      <ul>
      <% @blocker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :blocker %><br>
    <%= f.text_field :blocker %>
  </div>

  <%= f.hidden_field :cohort_id, value: @cohort.id if @cohort %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
