<section class="half">
<<<<<<< HEAD
<h1> Cohort/Show</h1>
</section>


<section class="half">
=======
<p>
  <h1><%= @cohort.name %></h1>
</p>

<p>
  <strong>Description:</strong>
  <%= @cohort.description %>
</p>
>>>>>>> 8181ecfb9e869bbc1cc75cd28a9a2c0fbac49228

<%= link_to 'Back', root_path %>

  <h1><%= @cohort.name %></h1>
 <h5> <%= @cohort.description %> </h5>

</section>


<section class="half">

<h1>Units</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Cohort</th>
      <th colspan="3"></th>
    </tr>
   <% @units.each do |unit| %>
      <% if unit.cohort.name == @cohort.name %>
      <tr>
        <td><%= link_to unit.name, unit_path %></td>
        <td><%= unit.description %></td>
        <td><%= unit.cohort.name %></td>
        <td><%= link_to 'Edit', edit_unit_path(unit) %></td>
      </tr>
      <% end %>
    <% end %>
  </tbody>
</table>
<% if @current_user.role(@cohort).title == "Instructor" %>
 <%= link_to 'New Unit', new_cohort_unit_path(@cohort.id) %>
<%end%>
</section>